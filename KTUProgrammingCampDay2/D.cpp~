#include <iostream>
#include <cstdio>
#include <cstring>
#include <algorithm>
#include <queue>

using namespace std;

const int maxn = 2e5 + 100;

struct node{
    int val, id;
    node(int _val = 0, int _id = 0) {
	val = _val;
	id = _id;
    }
    
};
int cmp(node a, node b) {
    return a.val < b.val;
}
node a[maxn];
queue<int> q;
void solve() {
    while(!q.empty()) {
	
    }
}

int main() {
    freopen("D.txt", "r", stdin);
    
    return 0;
}
