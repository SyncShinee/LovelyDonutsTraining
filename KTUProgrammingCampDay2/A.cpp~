#include <cstdio>
#include <algorithm>
#include <cstring>
using namespace std;
int main() {
    int a1, b1, a2, b2;
    scanf("%d%d%d%d", &a1, &b1, &a2, &b2);
    if (a1 * b1 < a2 * b2) {
	swap(a1, a2);
	swap(b1, b2);
    }
    if (a1 <= b1 && a2 <= b2) {
	
    }
    else if (a1 <= b2 && b1 <= a2) {

    }
    else {
	
    }
    return 0;
}
